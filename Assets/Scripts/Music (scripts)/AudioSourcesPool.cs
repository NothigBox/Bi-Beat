using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AudioSourcesPool
{
    /*
    private Queue<AudioSource> lightSources;
    private Queue<AudioSource> darkSources;

    
    public AudioSource GetLightSource(GameObject gameObject)
    {
        AudioSource result = default;
        
        if(lightSources.Count <= 0)
        {
            AudioSource source = gameObject.AddComponent<AudioSource>();
            source.loop = false;
            lightSources.Enqueue(source);
        }

        return lightSources.Dequeue();
    }
    
    public AudioSource GetDarkSource(GameObject gameObject)
    {
        
        AudioSource result = default;
        
        if(darkSources.Count <= 0)
        {
            AudioSource source = gameObject.AddComponent<AudioSource>();
            source.loop = false;
            darkSources.Enqueue(source);
        }

        return darkSources.Dequeue();
    }
    */

    
}
